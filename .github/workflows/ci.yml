name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Run tests
        run: python -m unittest discover -s tests -v
      - name: Smoke-test CLI
        run: |
          python -m trading_bot generate-data --out /tmp/demo.csv --bars 200
          python -m trading_bot backtest --data /tmp/demo.csv --strategy sma
          python -m trading_bot paper --bars 20 --no-realtime --seed 1 --state /tmp/paper.json
